starship init fish | source
cat ~/.cache/wal/sequences &
neofetch --disable gpu term_font cpu memory --ascii_distro manjaro_small
set fish_greeting

set -x SSH_AUTH_SOCK '/run/user/1000/gnupg/S.gpg-agent.ssh'
